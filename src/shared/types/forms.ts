export type Iform = {
    label : string,
    type : string,
    maxLength? : number,
    minLength? : number,
    disabled : Boolean,
    placeHolder? : string,
    required : Boolean,
    valueName : string
}